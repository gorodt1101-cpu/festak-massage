# Деплой

## Подготовлено

Дата: 12.07.2026

1. `(x)` Финальный лендинг скопирован в `index.html`, чтобы Vercel открывал сайт с главной страницы.
2. `(x)` Папка `assets` содержит изображения сайта:
   - `festak-mask.jpg`
   - `festak-certificate.png`
   - `festak-master.jpeg`
3. `(x)` Проверено, что Vercel CLI доступен: версия `55.0.0`.

## Статус деплоя

`(x)` Сайт задеплоен на Vercel.

Production URL:

```text
https://festak-massage.vercel.app
```

Deployment URL:

```text
https://festak-massage-46pu6yr2p-gorodt1101-3714s-projects.vercel.app
```

Inspect URL:

```text
https://vercel.com/gorodt1101-3714s-projects/festak-massage/8Cb9rUngHLfE1QNLB82NTTWvp7Cc
```

## Проверка после деплоя

1. `(x)` Опубликованный сайт открывается.
2. `(x)` `title` корректный: «Фестак — премиальный африканский массаж и SPA в Москве».
3. `(x)` `h1` корректный.
4. `(x)` Битых изображений не найдено.
5. `(x)` На мобильной ширине 390 px горизонтального overflow нет.

Скриншот:

```text
C:\Users\User\Documents\Website\test-output\festak-vercel-mobile.png
```
